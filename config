# Config file so that `ssh pi` goes straight to Raspberry Pi
# The Pi has had its Static IP set on the device to what's listed
# Since the RPi5 is the main network node, it will be just 'pi'
# The RPi4 will use the wlan0 IP address

Host pi5
    HostName 192.168.1.10
    User kyle
    IdentityFile ~/.ssh/id_ed25519

Host pi4
    HostName 192.168.1.13
    User kyle
    IdentityFile ~/.ssh/id_ed25519
